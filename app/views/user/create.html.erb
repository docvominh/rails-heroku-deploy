<div class="container" style=" padding-top: 100px;">
  <p>Find me in app/views/user/create.html.erb</p>

  <%= error_messages_for(@user) %>
  <%= form_for @user, :validate => true, :url => url_for(:controller => 'user', :action => 'create_user') do |m| %>

      <%= m.label :user_id %>
      <%= m.text_field :user_id, :class => 'form-control' %>

      <br>
      <%= m.label :user_name %>
      <%= m.text_field :user_name, :class => 'form-control' %>

      <br>
      <%= m.label :password %>
      <%= m.password_field :password, :placeholder => "Password", :class => 'form-control' %>

      <br>
      <%= m.label :password_confirmation %>
      <%= m.password_field :password_confirmation, :placeholder => "Password confirm field", :class => 'form-control' %>

      <br>
      <%= m.label :date_of_birth %>
      <!--<%= m.date_select(:date_of_birth, :default => Time.now, :order => [:month, :day, :year], start_year: Date.today.year - 18, end_year: Date.today.year - 100, :html => {:class => "form-control"}) %>-->
      <%= m.date_select(:date_of_birth, {:order => [:month, :day, :year], :start_year => 1975, :prompt => {:month => 'Month', :day => 'Day', :year => 'Year'}}, :class => "form-control date-select") %>

      <br>
      <%= m.label :email %>
      <%= m.text_field :email, :class => 'form-control' %>

      <br>
      <%= m.label :note %>
      <%= m.text_field :note, :class => 'form-control' %>

      <%= m.submit :value => 'Create', :class => 'btn btn-lg btn-success btn-block' %>
  <% end %>

  <%= form_for @user_image, :remote => true,:format => 'js', :url => url_for(:controller => 'user', :action => 'upload_user_image'), html: {multipart: true, id: 'imageForm'} do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <br>
      <%= f.label :attachment %>
      <%= f.file_field :attachment %>
      <br>
      <%= f.submit 'Upload', id: 'image-upload', class: 'btn btn-primary' %>
      <!--<button type="button" id="image-upload" class="btn btn-primary">Upload</button>-->
  <% end %>


</div>
<script>

  $(function () {

    $('#imageForm').on("ajax:success", function () {
      console.log('fuck')
      if ( $(this).data('remotipartSubmitted') ){
        console.log('fuck 2')
      }
    });

    $('#imageForm').on("ajax:remotipartComplete", function(e, data){
      console.log(e, data)
    });


  });
</script>