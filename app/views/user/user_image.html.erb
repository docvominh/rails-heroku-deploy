<div class="container" style=" padding-top: 100px;">
  <% if !flash[:notice].blank? %>
      <div class="alert alert-info">
        <%= flash[:notice] %>
      </div>
  <% end %>

  <br/>

  <!--<%= link_to "New Image", new_resume_path, class: "btn btn-primary" %>-->

  <a class="btn btn-primary" href="<%= url_for(:controller => 'user', :action => 'upload_user_image_index') %>">New Image</a>
  <br/>
  <br/>

  <table class="table table-bordered table-striped">
    <thead>.
    <tr>
      <th>Name</th>
      <th>Download Link</th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <% @all_image.each do |image| %>

        <tr>
          <td><%= image.name %></td>

          <td><%= link_to "Download Resume", image.attachment_url %></td>
          <td>
            <a data-method="delete" data-confirm="Are you sure?" href="<%= url_for(:controller => 'user', :action => 'delete_image', :id => image.id) %>">
              <button class="btn btn-danger"><span class="glyphicon glyphicon-remove"></span>Delete</button>
            </a>
          </td>
        </tr>

    <% end %>
    </tbody>

  </table>

</div>